[package]
name = "bilibili-cache-decryption-tool"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[[bin]]
name = "bcdt"
path = "src/main.rs"

[dependencies]
clap = { version = "^4", features = ["derive"] }
